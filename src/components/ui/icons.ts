/**
 * AIPE Icon System — Phosphor Icons
 * https://phosphoricons.com
 *
 * Weight guide:
 *   regular  — default functional / utility
 *   bold     — active / selected / emphasis
 *   fill     — status indicators, solid states
 *   duotone  — decorative / hero / brand marks
 */
export type { Icon as PhosphorIcon } from "@phosphor-icons/react";

export {
  // ── Navigation ────────────────────────────────────────────
  SquaresFour,     // Dashboard
  ChartBar,        // Analytics
  Lightbulb,       // Insights
  Palette,         // Personalize
  PencilSimple,    // Live Editor
  Stack,           // Themes
  GearSix,         // Settings

  // ── App chrome ────────────────────────────────────────────
  Brain,           // AI logo / brand mark
  CaretLeft,       // Sidebar collapse
  CaretRight,      // Expand / breadcrumb divider
  Sparkle,         // AI sparkle / recommendations
  Command,         // ⌘ keyboard shortcut
  Bell,            // Notifications
  MagnifyingGlass, // Search
  Lightning,       // Adapt / quick action

  // ── Actions ───────────────────────────────────────────────
  ArrowCounterClockwise, // Refresh / Reset
  DownloadSimple,        // Export / Download
  Trash,                 // Delete
  Check,                 // Confirm
  X,                     // Close / Dismiss

  // ── Status ────────────────────────────────────────────────
  CheckCircle,     // Success / all-clear
  XCircle,         // Error / failed
  Warning,         // Warning / alert
  Info,            // Informational

  // ── Trends / data ─────────────────────────────────────────
  TrendUp,         // Positive delta
  TrendDown,       // Negative delta
  Minus,           // Neutral / flat
  ArrowUpRight,    // External link / expand

  // ── Interface tokens ──────────────────────────────────────
  Clock,           // Timestamp
  Eye,             // View / typography
  Cursor,          // Mouse / navigation style
  Pulse,           // Motion / cognitive load (replaces Activity)
  Database,        // Data management
  Shield,          // Privacy / security
  Keyboard,        // Keyboard shortcuts

  // ── Device preview ────────────────────────────────────────
  Desktop,         // Monitor / desktop preview
  DeviceTablet,    // Tablet preview
  DeviceMobile,    // Mobile preview

} from "@phosphor-icons/react";
